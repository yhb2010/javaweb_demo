encodeURI()
	用于对url编码的函数。它可以将整个url中的字符（除了! # & $ ' ( ) * + , - . / : ; = ? @ _ ~ 0-9 a-z A-Z）进行utf-89编码，在
	每个字符前加上%。
encodeURIComponent()
	这个函数比encodeURI()编码还要彻底，他除了对! ' ( ) * - . _ ~ 0-9 a-z A-Z这个几个字符不编码，对其它所有字符都编码。这个函数通常
	用于将一个url作为一个参数放在另一个url中。