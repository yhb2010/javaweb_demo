utf-8采用了一种变长的技术，每个编码区域有不同的字码长度，不同类型的字符可以由1-6个字节组成。
	1、如是1个字节，最高位（第8位）是0，则表示这是一个ascii字符（00-7f），可见，所有ascii编码已经是utf-8了。
	2、如果是一个字节，以11开头，则连续的1的个数暗示这个字符的字节数，例如110xxxxx代表它是双字节utf-8字符的首字节。
	3、如果是一个字节，以10开始，表示它不是首字节，则需要向前查找才能得到当前字节的首字节。